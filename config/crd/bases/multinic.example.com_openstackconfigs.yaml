---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: openstackconfigs.multinic.example.com
spec:
  group: multinic.example.com
  names:
    kind: OpenstackConfig
    listKind: OpenstackConfigList
    plural: openstackconfigs
    singular: openstackconfig
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OpenstackConfig is the Schema for the openstackconfigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of OpenstackConfig
            properties:
              credentials:
                description: credentials contains provider and project identifiers.
                properties:
                  k8sProviderID:
                    description: k8sProviderID is required and used as the providerId
                      header for Viola routing.
                    minLength: 1
                    type: string
                  openstackProviderID:
                    description: openstackProviderID is the provider ID used by Contrabass
                      API.
                    minLength: 1
                    type: string
                  projectID:
                    description: projectID is the OpenStack project (tenant) ID.
                    minLength: 1
                    type: string
                required:
                - k8sProviderID
                - openstackProviderID
                - projectID
                type: object
              secrets:
                description: secrets references sensitive values required by this
                  CR.
                properties:
                  contrabassEncryptKeySecretRef:
                    description: contrabassEncryptKeySecretRef provides encrypt key
                      via Secret.
                    properties:
                      key:
                        description: key is the Secret data key.
                        minLength: 1
                        type: string
                      name:
                        description: name is the Secret name.
                        minLength: 1
                        type: string
                    required:
                    - key
                    - name
                    type: object
                type: object
              settings:
                description: settings overrides operator-level defaults for this CR.
                properties:
                  contrabassEncryptKey:
                    description: |-
                      contrabassEncryptKey is used for decrypting adminPw.
                      NOTE: SecretRef 사용을 권장한다.
                    type: string
                  contrabassEndpoint:
                    description: contrabassEndpoint is the base URL for Contrabass
                      API.
                    type: string
                  contrabassInsecureTLS:
                    description: contrabassInsecureTLS allows insecure TLS.
                    type: boolean
                  contrabassTimeout:
                    description: contrabassTimeout is the HTTP timeout (e.g. 30s).
                    type: string
                  downPortFastRetryMax:
                    description: downPortFastRetryMax controls fast retry count for
                      DOWN ports.
                    format: int32
                    type: integer
                  violaEndpoint:
                    description: violaEndpoint overrides the operator-level Viola API
                      endpoint.
                    type: string
                  openstackEndpointInterface:
                    description: openstackEndpointInterface selects endpoint interface
                      (public/internal/admin).
                    type: string
                  openstackEndpointRegion:
                    description: openstackEndpointRegion selects endpoint region.
                    type: string
                  openstackInsecureTLS:
                    description: openstackInsecureTLS allows insecure TLS.
                    type: boolean
                  openstackNeutronEndpoint:
                    description: openstackNeutronEndpoint overrides neutron endpoint.
                    type: string
                  openstackNodeNameMetadataKey:
                    description: openstackNodeNameMetadataKey overrides nodeName mapping
                      metadata key.
                    type: string
                  openstackNovaEndpoint:
                    description: openstackNovaEndpoint overrides nova endpoint.
                    type: string
                  openstackPortAllowedStatuses:
                    description: openstackPortAllowedStatuses filters port statuses
                      (e.g. ACTIVE, DOWN).
                    items:
                      type: string
                    type: array
                  openstackTimeout:
                    description: openstackTimeout is the HTTP timeout (e.g. 30s).
                    type: string
                  pollErrorInterval:
                    description: pollErrorInterval is the retry interval on error.
                    type: string
                  pollFastInterval:
                    description: pollFastInterval is the fast polling interval.
                    type: string
                  pollFastWindow:
                    description: pollFastWindow is the fast polling window after changes.
                    type: string
                  pollSlowInterval:
                    description: pollSlowInterval is the slow polling interval.
                    type: string
                type: object
              subnetIDs:
                description: |-
                  subnetIDs is the list of OpenStack subnet IDs to target.
                  subnetIDs가 있으면 subnetID/subnetName을 무시한다.
                items:
                  type: string
                type: array
              subnetID:
                description: |-
                  subnetID is the OpenStack subnet ID to target when subnetIDs is empty.
                  subnetID가 우선이며, 없으면 subnetName을 사용한다.
                type: string
              subnetName:
                description: |-
                  subnetName is the OpenStack subnet name to target when subnetID is empty.
                  서브넷명이 중복되면 오류가 발생할 수 있으므로 subnetID 사용을 권장한다.
                type: string
              vmNames:
                description: vmNames is the list of OpenStack VM IDs to configure.
                items:
                  type: string
                minItems: 1
                type: array
            required:
            - credentials
            - vmNames
            type: object
          status:
            description: status defines the observed state of OpenstackConfig
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the OpenstackConfig resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              downPortRetry:
                description: downPortRetry는 DOWN 포트 재전송 상태를 기록한다.
                properties:
                  fastAttempts:
                    description: fastAttempts는 빠른 재시도 구간에서 누적된 시도 횟수이다.
                    format: int32
                    type: integer
                  hash:
                    description: hash는 DOWN 포트 목록(정렬된 포트 ID)의 해시값이다.
                    type: string
                  lastAttempt:
                    description: lastAttempt는 마지막 전송 시각이다.
                    format: date-time
                    type: string
                type: object
              lastError:
                description: lastError records the latest error message if the reconcile
                  failed.
                type: string
              lastSyncedAt:
                description: lastSyncedAt is the last time the operator successfully
                  synced data.
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
